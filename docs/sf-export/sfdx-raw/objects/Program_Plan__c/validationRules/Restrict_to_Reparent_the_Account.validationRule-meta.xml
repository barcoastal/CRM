<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Restrict_to_Reparent_the_Account</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    NOT( $Permission.Bypass_Validation_Rule ),
    OR(
        ISCHANGED( Client__c ),
        ISCHANGED( Opportunity__c )
    ),
    Client__c &lt;&gt; Opportunity__r.AccountId
)</errorConditionFormula>
    <errorMessage>Program Plan cannot be reparented to different Account</errorMessage>
</ValidationRule>
